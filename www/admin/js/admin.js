// Désiré VPN Connect - Administration

console.log("Panneau administrateur chargé");

const boutons = document.querySelectorAll("button");

boutons.forEach((bouton) => {
    bouton.addEventListener("click", () => {
        alert("Fonction administrateur en préparation...");
    });
